# URL do motor de distribuição para onde os leads do formulário são enviados.
# Usado no server (src/app/api/lead/route.ts). Se ausente, usa o default abaixo.
LEADS_WEBHOOK_URL=https://distribuicao.institutoareluna.pt/backend/webhooks/leads/site
